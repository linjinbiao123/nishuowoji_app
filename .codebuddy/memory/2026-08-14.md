# 2026-08-14

- 调整预算页月预算卡片：大字显示「本月预算金额」，下方改为红色加粗的「本月剩余金额」；超支时仍显示「已超支 ¥xxx」红色。
- 首页流水与历史账单记录项新增账户标签：根据 `Record.accountId` 解析账户名，显示为主色淡底圆角小标签；未选账户则不显示。
- 默认账户去掉「银行卡」，仅保留「微信」「支付宝」；新增账户默认图标改为钱包图标（`0xE850`），替换原来的问号图标（`0xE88A`）。
- 设置页「检查更新」弹窗：检测到新版本时除了显示更新说明，还会展示 `version.json` 里的下载链接（蓝奏云链接），支持一键复制，并保留「去下载」用浏览器打开。
