<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 发送通知（Android 13+ 需要，运行时还会再动态申请一次） -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- 开机/应用更新后重新登记每日提醒 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- 精确闹钟：保证提醒在设定时刻准时触发 -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <!-- 语音记账：麦克风录音（运行时还会动态申请） -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- 语音记账：调用云端语音识别接口（仅此功能联网，其余功能完全离线） -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:label="说记"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- 快捷记账弹窗：透明 Activity，浮在任意界面之上只显示底部记账卡片。
             taskAffinity 独立 + excludeFromRecents，确保它不会带着主界面一起出现在
             最近任务列表里（用户感知是"弹窗"而非"打开了一个 App 页面"）。 -->
        <activity
            android:name=".QuickAddActivity"
            android:exported="false"
            android:theme="@style/QuickAddTheme"
            android:launchMode="singleTop"
            android:taskAffinity="com.example.nishuowoji_app.quickadd"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true" />

        <!-- 快捷设置磁贴（通知栏下拉的"快速记账"开关）。
             API 24 才支持，minSdk 保持 23 不影响：低版本系统根本没有磁贴功能，
             不会去加载这个 Service。 -->
        <service
            android:name=".QuickAddTileService"
            android:exported="true"
            android:label="@string/quick_add_tile_label"
            android:icon="@drawable/ic_qs_quick_add"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- 定时通知接收器：到点后由它真正弹出通知（插件 v16 起需应用自行注册） -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false" />
        <!-- 开机/应用更新后重新排定所有定时通知 -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
